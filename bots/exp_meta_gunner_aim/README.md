# Reactive home-gunner aiming

Parent: frozen `exp_trans_40`. This independently tests only v6's omitted
home-gunner steering: slot 15 publishes the exact verified core threat, and a
friendly gunner that can hit it may rotate at a 25-Ti floor. Ammo conversion,
builders, siege, economy, spawning, and generic gunner scoring are unchanged.
